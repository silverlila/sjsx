# SJSX - Stream JSX

**SJSX** (Stream JSX) is a learning project that implements a complete JSX runtime and server-side renderer from scratch. It's built on Web Standards, has minimal dependencies, and focuses on understanding how JSX, SSR, and streaming work internally.
